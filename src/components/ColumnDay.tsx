import { Data3Hrs } from "../interfaces/ForecastInterfaces";
//import Column3Hrs from './Column3Hrs';

//const random = (): string => (Math.random() + 1).toString(36).substring(7)

export default function ColumnDay({ ...columns3Hrs }: Data3Hrs[]) {
    
    return (
        <></>
    )
}